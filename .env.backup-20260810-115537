# Local backend, serving the sman_migration_proto database (migrated legacy
# data). Restore the production line below to point back at live:
# VITE_SERVER_URL=https://sman-backend.onrender.com
VITE_SERVER_URL=http://localhost:5002
# Test/staging only: shows the invoice "Simulate payment" button. Leave unset
# (or "false") in production — the backend rejects the call outside test mode.
VITE_ENABLE_DEV_PAYMENT=false
