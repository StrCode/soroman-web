/**
 * The dashboard's editorial vocabulary, shared by every authed page so the
 * surfaces can't drift apart: PANEL is the raised section card, MICRO the
 * wide-tracked uppercase label used in panel headers.
 */
export const PANEL =
	"overflow-hidden rounded-xl border border-foreground/15 bg-background shadow-[0_8px_32px_rgba(0,0,0,0.06)]";

export const MICRO = "text-xs tracking-[0.25em] uppercase";
